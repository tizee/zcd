[package]
name = "fuzzy"
version = "0.2.0"
edition = "2021"
description = "Typo-tolerant fuzzy matching engine based on fzy algorithm"
license = "MIT"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
